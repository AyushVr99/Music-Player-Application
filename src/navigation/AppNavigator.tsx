import React from 'react';
import BottomTabNavigator from './BottomTabNavigator';

// You can wrap with Drawer here if needed
export default function AppNavigator() {
  return <BottomTabNavigator />;
}